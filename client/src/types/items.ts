export interface RoomItem {
    id: string
    room_id: string
    name: string
    description: string
    price: number
    purchase_date: string
    updated_at: string
    created_at: string
    invoice_url: string
    image_url: string
    owner_id: string
}
